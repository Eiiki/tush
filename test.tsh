def func()
    var x,y;
    x = 10;
    y = 5;
    #this is a comment

    while(y < 10)
        y = y - 1;
        if(x > y)
            x = x - 1;
        else
            x = x + 1;
        end;
    end;
    return x;
end
