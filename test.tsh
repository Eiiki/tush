def func()
    var x;
    var y;
    x = 10;
    y = 5;

    if (x > 5)
        x = 5;
    elsif (y < x)
        x = y;
    else
        x = y = 1;
    end;

    y = 10;
    while(y >= 0)
        y = y - 1;
        x = x + 1;
    end;

    return sum(x,y);
end

def sum(a, b)
    return a + b;
end